import React,{useState} from 'react'

const Slot = () => {
    /*const[daySlot,setDaySlot]=useState(["11:00am","12:00pm","1:00pm","2:00pm","3:00pm","4:00pm","5:00pm","6:00pm"])
    const[date,setDate]=useState(new Date() || "")
    const date2=date.toUTCString()
    const dateString=(date2)=>{
        const day=date2.substr(0,3)
        const number=date2.substr(5,2)
        const month=date2.substr(8,3)
        const string1=day+" "+number+" "+month;
        setDate(string1)
    }
    console.log(dateString())
    console.log(date)*/
  return (
    <div>
      <div className='top'>
        <p className='date'>Yo</p>
      </div>
    </div>
  )
}

export default Slot
